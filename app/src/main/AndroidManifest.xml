<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bsc.gri.dietappx">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".DiyetimActivity" />
        <activity android:name=".DiyetSecimActivity" />
        <activity android:name=".ProfilimActivity" />
        <activity android:name=".KaloriHesaplamaActivity" />
        <activity android:name=".ProfilEditActivity" />
        <activity
            android:name=".RealMainActivity"
            android:label="@string/title_activity_real_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>